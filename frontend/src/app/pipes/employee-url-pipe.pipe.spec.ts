import { EmployeeUrlPipePipe } from './employee-url-pipe.pipe';

describe('EmployeeUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
