import { CompanyUrlPipePipe } from './company-url-pipe.pipe';

describe('CompanyUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
